// components/RecommendedTag/RecommendedTag.jsx
import styles from './recommendedTag.module.css';

const RecommendedTag = () => {
  return <span className={styles.tag}>Recommended</span>;
};

export default RecommendedTag;